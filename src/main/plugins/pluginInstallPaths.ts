/** Host-owned directories kept outside installed package discovery. */
export const PLUGIN_STAGING_DIR = '.navide-staging'
export const PLUGIN_QUARANTINE_DIR = '.navide-quarantine'
/** Durable Host-owned package/router/storage selector records. */
export const PLUGIN_ACTIVATION_DIR = '.navide-lifecycle'
/** Marker used only when a forensic move cannot be completed. */
export const PLUGIN_QUARANTINE_MARKER = '.navide-quarantined.json'

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Inherited by nested executables (the PyInstaller backend, helpers).
       disable-library-validation lets the frozen backend load its bundled
       libpython and koffi load native libs at runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Chromium records audio in its utility (audio service) helper, which is
       signed with these inherited entitlements, not the main app's. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
